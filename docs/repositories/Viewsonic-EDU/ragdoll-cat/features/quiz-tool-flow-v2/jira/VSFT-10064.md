<!--
==============================================================
SOURCE TRACKING — 更新來源後請同步更新此區塊與內文
==============================================================

issue_key:      VSFT-10064
url:            https://viewsonic-vsi.atlassian.net/browse/VSFT-10064
issue_type:     故事
status:         STAGE READY (READY FOR QA)（進行中）
comments:       2
cloned_version: 1
cloned_at:      2026-09-03

Maintenance rule: 重新 clone 前先看 git diff，若本機有補充註解要先 commit；
                  版號與 cloned_at 一起更新，commit 訊息附來源 URL。
                  取得方式：python3 scripts/clone-atlassian.py …（見該檔 docstring）
==============================================================
-->

> | 來源 issue | issue_key | clone 版本 | clone 日期 |
> |---|---|---|---|
> | [VSFT-10064](https://viewsonic-vsi.atlassian.net/browse/VSFT-10064) | VSFT-10064 | v1 | 2026-09-03 |

# VSFT-10064

## Summary

[Quiz Tool][Windows] 截圖遮罩 UI 改版：預置框、題型面板、設定移入

## Metadata

| 欄位 | 值 |
| --- | --- |
| Status | STAGE READY (READY FOR QA)（進行中） |
| Issue Type | 故事 |
| Priority | Medium |
| Assignee | Sean Yeh (sean.ch.yeh@viewsonic.com) |
| Reporter | Fred (fred.cy.lin@viewsonic.com) |
| Created | 2026-08-26 11:40:26 |
| Updated | 2026-09-02 17:03:00 |
| Labels | buyhouse-test |

## Description

**Spec：**[2. Question 截圖遮罩（Scope 2）](https://viewsonic-vsi.atlassian.net/wiki/spaces/myViewboar/pages/606797937)（v23）
**總覽：**[Quiz Tool 啟動流程改版 — Spec](https://viewsonic-vsi.atlassian.net/wiki/spaces/myViewboar/pages/606797917)

**平台**：Windows（whiteboard mode 與 desktop mode 皆須涵蓋）

---

# ⚠️ 規格已變更（本票開工之後）

本票目前在 **IN CODE REVIEW**，但 spec 已經過四輪變更。以下依時間倒序列出**與開工時不同**的地方。

## 🔴 2026-09-01 · 提示上的 ✕ 是取消整個截圖流程

|  | 舊（作廢） | **新** |
|---|---|---|
| `SC-6` 按提示上的 ✕ | 「提示消失，框與後續流程**不受影響**」—— 只關掉那條提示，老師仍留在遮罩裡 | **取消整個截圖流程**：遮罩關閉、回到工具列，框選一併丟棄。**行為同 Cancel，也不跳確認框** |
| `SC-6a` | 「提示已被 ✕ 關掉 → 老師動框 → Panel 照常出現」 | 該狀態**不存在了**。改寫為：提示因動框而消失後，**沒有 ✕ 可按**，退出改走 footer 的 **Cancel**（`SC-5`）或 **Esc**（`SC-15`） |

**這是行為反轉，不是補充** —— 若已實作成「只關提示」，這一條要改。

**退出遮罩一共三條路，行為完全相同（直接關閉、丟棄框選、不跳確認框）：**

| 入口 | 什麼時候看得到 |
|---|---|
| 提示上的 **✕**（`SC-6`） | 面板出現**前** |
| **Cancel**（`SC-5`） | 面板出現**後**（footer 上） |
| **Esc**（`SC-15`） | 遮罩顯示中**任何時候** |

⚠️ 連帶影響：提示消失後，footer 的 Cancel 是**唯一**的退出入口，所以 `SC-9`（Panel 碰到畫面上下緣往回推）不只是美觀問題 —— **Cancel 被切出畫面等於老師沒辦法離開**。

## 🔢 2026-08-28 · AC 編號改為 `SC-xx`

子頁 2 的 AC 編號前綴由 `S2-` 改為 `SC-`（Screen Capture）。**數字完全不變**，舊引用把前綴換掉即可（`S2-15` → `SC-15`）。

**為什麼要改**：`S2-` 這個號池被子頁 2／3／4 三頁各自獨立編號，造成 **12 條同號不同義**。例如舊的 `S2-15` 在本票是「Esc 直接關閉遮罩」，在 `VSFT-10066`／`10067`（子頁 3）卻是「Enter class」。子頁 3 與子頁 4 之間沒有撞號，**所以只改子頁 2（本票）**，其他票的編號不動。

| 舊 | 新 | 本票的意思 |
|---|---|---|
| `S2-10` | `SC-10` | 雙螢幕 |
| `S2-11` | `SC-11` | 解析度改變 |
| `S2-12` | `SC-12` | 框的 150 px 下限 |
| `S2-13` | `SC-13` | 題型來回切換不記憶 |
| `S2-14` | `SC-14` | 面板長高時立即避讓 |
| `S2-15` | `SC-15` | Esc 直接關閉 |
| `S2-16` | `SC-16` | Capture 後遮罩立即關閉 |
| `S2-17` | `SC-17` | 框的最大尺寸與安全邊距 |
| `S2-18` | `SC-18` | 16:9 不鎖定 |
| `S2-25` | `SC-25` | 40 px 遲滯 |
| `S2-26` | `SC-26` | overlay 維持當前側 |
| `S2-27` | `SC-27` | 框變大不換邊 |

其餘未撞號者一併換前綴：`SC-3`／`SC-5`／`SC-6`／`SC-6a`／`SC-7`／`SC-8`／`SC-9`／`SC-31`。

⚠️ **純數字 AC**（`3`／`4`／`5`／`5a`／`5b`／`17`）是跨四頁的全域舊號，**維持不動**。

## 🔴 2026-08-28 下午 · PM 裁決兩條

| 項目 | 舊 | **新** |
|---|---|---|
| **選項標示〔ABC｜123〕** | 未定（列為未決 `Q6`），POC 未實作 | **要做** —— 設定段從兩段變**三段**，多一組 segmented：**ABC ｜ 123**，**預設 ABC** |
| **Cancel 的行為** | 跳「**Close question?**」確認框，按 Close 才離開 | **直接關閉遮罩，不跳任何確認框**。**確認框整個作廢，不實作** |

- 「Close question?」對話框**不用做**了 —— 若已實作請移除。**Figma 已同步從設計稿移除該對話框**
- 面板長版**變高了**（多一段設定），`SC-14`（長高當下立即避讓）與 `SC-9`（上下夾制）的重要性隨之提高
- ⚠️ **Figma 尚未繪製選項標示那一段** —— 在設計稿補上之前，以 spec 為準

## 2026-08-27／08-28 上午 · 前兩輪變更

| 項目 | 舊 | **新** |
|---|---|---|
| 預置框比例 | 16:9 **鎖定** | **16:9 只是預設值，不鎖定** —— 長寬可獨立調整 |
| 框的最小尺寸 | 未定義 | **150 × 150 px**，觸及下限就停住 |
| 框的最大尺寸 | 未定義 | **全螢幕減四邊各 16 px 安全邊距**，截圖內容不得超出 |
| 左右都放不下 Panel 時 | 貼畫面右緣、覆蓋框的右側 | **overlay 在框內，維持當前所在側**；**不因框變大而主動換邊** |
| 防抖動 | 無 | **40 px 遲滯**：右→左 `右側可用空間 < Panel 寬 + 24`；左→右 `> Panel 寬 + 24 + 40` |
| 截圖框會不會被推開 | 未明言 | **截圖框不主動位移**，讓位的永遠是 Panel |

**若已實作「貼右緣」的避讓邏輯，這一條是反方向的改動，不是補充。**

---

## 截圖框（Screenshot Frame）規格

| 項目 | 值 |
|---|---|
| 預設尺寸 | **748 × 421 px** |
| 預設位置 | **水平置中** |
| 預設比例 | **16:9，不鎖定**（長寬可獨立調整） |
| 最小寬／高 | **150 px ／ 150 px** |
| 最大尺寸 | **全螢幕，四邊各保留 16 px 安全邊距** |

## Panel 設定段（僅選擇題／投票題，2026-08-28 起為三段）

| 段 | 選項 | 預設 |
|---|---|---|
| **Answer options** | 2 顆膠囊。文案依題型分歧：選擇題＝ Single-select／Multi-select；投票題＝ Single-vote／Multi-votes | 第一顆 |
| **Number of options** | 圓形 chip：2 · 3 · 4 · 5 · 6 | **2** |
| **選項標示**（新增） | segmented：**ABC ｜ 123** | **ABC** |

切到 **123** 時，選項標示由 A/B/C… 改為 1/2/3…，**學生端作答畫面與結果頁一致採用該標示**。

其餘四型（是非題、Audio、Short answer、Sketch response）**沒有設定段**。題型來回切換時三段**都回到預設值，不記憶**（`SC-13`）。

## Panel 位置規則

**前提：Panel 為浮動元件，截圖框不主動位移。**

**A. 左右**（依序判斷，第一條成立就停）

1. `框右邊緣 + Panel 寬 + 24 ≤ 視窗寬` → 放**右側**，間距 24 px
2. `框左邊緣 − Panel 寬 − 24 ≥ 0` → 放**左側**，間距 24 px
3. 左右皆不符合 → **overlay 在框內，維持當前所在側**

**防抖動**：右→左門檻是 `< W+24`，左→右是 `> W+24+40` —— 兩個門檻**刻意不對稱**，避免老師把框拖到臨界點時面板來回抖動。

**B. 上下**（一律套用）：垂直對齊框中線，**碰到畫面上下緣就往回推**。允許壓在標題列／工具列上，但**不允許任何一部分落在畫面外**。

> A 組只改 x，沒有任何一條會動到 y。把面板拉出畫面的正是「對齊框中線」本身 —— B 不能省。

## 驗收條件

依 spec 子頁 2 的 5 個 story：

- **Story 1 進入遮罩**：`3`、`SC-17`（最大尺寸與安全邊距）、`SC-18`（比例不鎖定）、`SC-6`（提示 ✕ ＝取消截圖流程 · 🔴 已改寫）、`SC-6a`（提示消失後改走 Cancel · 🔴 已改寫）、`17`（可錄影）、`SC-10`（雙螢幕）、`SC-11`（解析度改變）
- **Story 2 動框之後**：`4`、`5b`、`SC-12`（150 px 下限）
- **Story 3 設定段**：`5`（三段一起長出）、`5a`（三段一起收回）、`SC-31`（選項標示 ABC／123 · 🔴 新增）、`SC-3`、`SC-13`（題型來回切換不記憶）
- **Story 4 Panel 位置**：`SC-7`、`SC-25`（40 px 遲滯）、`SC-26`（overlay 維持當前側）、`SC-27`（框變大不換邊）、`SC-9`、`SC-8`、`SC-14`（面板長高時立即避讓）
- **Story 5 出口**：`SC-5`（Cancel 直接關閉 · 🔴 已改寫）、`SC-15`（Esc 直接關閉 · 🔴 已改寫）、`SC-16`（Capture 後遮罩立即關閉）

- ⚠️ 原 `SC-5a`（確認框內的兩顆按鈕）**作廢**

## 數值的分工

**視覺樣式**（色票、字級、圓角、陰影、元件 token）**以 Figma 為準**，spec 不抄。但**行為門檻**（框的尺寸上下限、16 px 安全邊距、24 px 間距、40 px 遲滯）**寫在 spec 裡** —— 40 px 遲滯這種東西在靜態設計稿上根本看不出來。

## Figma

file `4C21d9puOZJcUyUR26oibd`（VSDS UI Design – ClassSwift Toggle），node 對照表在 spec 子頁 2。⚠️ **每次改版前重新確認 node-id**。

**設計稿狀態：**

- `22492:361313`（長版面板）**尚缺「選項標示」那一段** —— 在補上之前以 spec 為準
- `22269:21854`（提示 Adorning Menu）—— 外觀不變，但**其 ✕ 的語意已改為取消截圖流程**
- 原 Close question? 對話框（`22288:89978`）**已自設計稿移除**

> ⚠️ node `22492:359014` 是**設計稿錯誤**（Multiple choice 選中卻沒有設定段），不可作為規格依據。

## 注意

- **Sketch add more question 暫時不做**（決策 #11），該入口不實作
- `Q6`（選項標示）已結案 → 要做
- **兩點待 PM 補確認**：① 選項標示的預設值是 ABC 還是 123（spec 暫定 **ABC**）；② **投票題**是否同樣提供此設定（spec 暫定**提供**）
- 本票**尚未估點** —— Android 對應票 `VSFT-10065` 為 8 點，可作為參考；本輪新增的選項標示會增加工作量，扣掉確認框則減少
- 與 `VSFT-10091`（出題視窗 resize）的邊界待釐清：本票管的是**截圖框**的 resize，10091 若指的是 **Quiz 視窗本體**的 resize 則為另一件事

## Comments（2 則）

### 2026-08-28 10:58:21 · Jessi

點擊中間的白色區域可以拖動
四個角的按鈕只有真的點到的時候才會變成 Press 狀態
拖曳過程中，quiz 選單面板會關起來，放開拖曳後才會出現
quiz 選單面板基本上和截圖位置水平置中
要注意 IFP上的使用

### 2026-09-02 10:20:16 · Eddie Jhang

**已簽章上版 ✅**

- 環境：stg
- 版本：1.70.1 (stage/1.70.1)
- 下載：[S3](https://classswift-launcher-aps1-stg.s3.ap-southeast-1.amazonaws.com/ClassSwift.zip) / [Dropbox](https://www.dropbox.com/scl/fo/vi5lqq0v6k3py0hyr3a7v/ANOELTeuo_JuhgNJDi2FFdk?rlkey=7w87h211u9e4c19jh5uh5q3ai&dl=0) / [GitHub Release](https://github.com/Viewsonic-EDU/balinese-cat/releases/tag/stage/1.70.1)
- Commit: b440bd05702a94e4841f76f1c677a6b45262b0bf
- Workflow: [run](https://github.com/Viewsonic-EDU/balinese-cat/actions/runs/33500230913)
