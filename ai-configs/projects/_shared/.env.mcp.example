# Shared MCP tokens (cross-project)
# These are written to ~/.env.mcp.shared by setup-env.sh
# Add the following line to your ~/.zshrc to load them:
#   [ -f ~/.env.mcp.shared ] && source ~/.env.mcp.shared

# Atlassian (Confluence + Jira)
CONFLUENCE_API_TOKEN=
JIRA_API_TOKEN=
